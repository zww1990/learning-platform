spring:
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        username: nacos
        password: nacos
        context-path: /nacos
        namespace: 
        file-extension: yml
      discovery:
        username: nacos
        password: nacos
        namespace: public


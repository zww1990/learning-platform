"更新操作"
type Mutation {
    "新增数据"
    createBook(book: CreateBook!): Book!
    "修改数据"
    updateBook(
        "图书主键"
        id: ID!,
        "输入：图书数据模型"
        book: UpdateBook!): Book
    "按主键删除数据"
    deleteById(id: ID!): Boolean
}

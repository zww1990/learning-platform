"查询操作"
type Query {
    "按主键查询图书数据"
    bookById(id: ID!): Book
    "查询所有图书数据"
    bookList: [Book]
    "分页查询作者数据"
    authorPage(first: Int, after: String): AuthorConnection
    "查询所有用户数据"
    userList: [User]
    "用户登录"
    login(loginInput: LoginInput!): LoginOutput!
}

type Author {
    id: ID
    firstName: String
    lastName: String
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: [Author]
}

type BookQuery {
    bookById(id: ID): Book
}

schema {
    query: BookQuery
}

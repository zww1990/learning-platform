server:
  port: 8082

spring:
  application:
    name: polaris-provider
  cloud:
    polaris:
      address: grpc://183.47.111.80:8091
      namespace: zww
      enabled: true
      discovery:
        enabled: true
        register: true

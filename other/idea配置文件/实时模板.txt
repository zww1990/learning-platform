1、生成类或方法注释
快捷键：***

**
 * $title$$param$$return$
 * @author $user$
 * @since $date$
 */

title: groovyScript("_1 ? _1 : _2", capitalize(spaceSeparated(methodName())), capitalize(spaceSeparated(className())))
param: groovyScript("def result = ''; _1.eachWithIndex { item, index -> result += System.lineSeparator() + ' * @param ' + item + ' ' + item + index.next() }; return result;", methodParameters())
return: groovyScript("_1 && _1 != 'void' ? System.lineSeparator() + ' * @return ' + _1 : null", methodReturnType())
user: user()
date: date("yyyy-MM-dd HH:mm:ss")
 
2、声明slf4j成员变量
快捷键：log

public static org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger($class_name$.class);
$END$

class_name: className()

3、生成junit5测试方法
快捷键：test

@org.junit.jupiter.api.Test
public void test$END$() {
    try {
        System.err.println();
    } catch (Exception e) {
        e.printStackTrace();
    }
}

4、将方法形参名称和值打印到 log.info
快捷键：logi

log.info($format$);

format: groovyScript("'\"' + _2 + '(): ' + (_1.isEmpty() ? '\"' : _1.withIndex().collect { el, index -> el + ' = {}' }.join(', ') + '\", ' + _1.withIndex().collect { el, index -> el }.join(', '))", methodParameters(), methodName())

5、声明@Autowired依赖注入成员变量
快捷键：auto

@org.springframework.beans.factory.annotation.Autowired
private $class_name$ $field_name$;

class_name: "java.lang.Object"
field_name: camelCase(class_name)

6、声明@GetMapping方法
快捷键：get

@org.springframework.web.bind.annotation.GetMapping("/$path$")
public Object $method_name$() {
    return "$return$";
}

method_name: "get"
path: groovyScript("_1.toLowerCase()", methodName())
return: methodName()

7、声明@PostMapping方法
快捷键：post

@org.springframework.web.bind.annotation.PostMapping("/$path$")
public Object $method_name$() {
    return "$return$";
}

method_name: "post"
path: groovyScript("_1.toLowerCase()", methodName())
return: methodName()

8、声明@PutMapping方法
快捷键：put

@org.springframework.web.bind.annotation.PutMapping("/$path$")
public Object $method_name$() {
    return "$return$";
}

method_name: "put"
path: groovyScript("_1.toLowerCase()", methodName())
return: methodName()

9、声明@DeleteMapping方法
快捷键：del

@org.springframework.web.bind.annotation.DeleteMapping("/$path$")
public Object $method_name$() {
    return "$return$";
}

method_name: "delete"
path: groovyScript("_1.toLowerCase()", methodName())
return: methodName()

10、生成try-catch语句
快捷键：tryc

try {
    $END$
} catch (Exception e) {
    log.error(e.getLocalizedMessage(), e);
}

11、快速添加 @RestController 和 @RequestMapping
快捷键：rest

@org.springframework.web.bind.annotation.RestController
@org.springframework.web.bind.annotation.RequestMapping("/$path$")

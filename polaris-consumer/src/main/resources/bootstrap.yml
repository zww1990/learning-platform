server:
  port: 8081

spring:
  application:
    name: polaris-consumer
  cloud:
    polaris:
      address: grpc://183.47.111.80:8091
      namespace: zww
      enabled: true
      discovery:
        enabled: true
        register: true

feign:
  httpclient:
    enabled: true
